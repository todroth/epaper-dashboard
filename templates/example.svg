<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="800" height="480" viewBox="0 0 800 480">
    <!-- White background -->
    <rect width="800" height="480" fill="white"/>

    <!-- Title -->
    <text x="400" y="100" font-size="48" font-weight="bold" text-anchor="middle" font-family="sans-serif">
        E-Paper Dashboard
    </text>

    <!-- Placeholders for token replacement -->
    <text x="400" y="200" font-size="36" text-anchor="middle" font-family="sans-serif">
        TIME_NOW
    </text>

    <text x="400" y="280" font-size="28" text-anchor="middle" font-family="sans-serif">
        Temperature: TEMP_NOW
    </text>

    <text x="400" y="340" font-size="24" text-anchor="middle" font-family="sans-serif">
        WEATHER_DESCRIPTION
    </text>

    <!-- Sunrise/Sunset -->
    <text x="200" y="420" font-size="20" font-family="sans-serif">
        ☀️ ↑ SUNRISE_TIME
    </text>

    <text x="500" y="420" font-size="20" font-family="sans-serif">
        🌙 ↓ SUNSET_TIME
    </text>
</svg>
